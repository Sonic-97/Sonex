import { DomainError } from '../shared-kernel'; export class KitchenDomainError extends DomainError { constructor(code: string, message: string, details?: Readonly<Record<string, unknown>>) { super(code, message, details); } } export const kitchenInvariant = (code: string, message: string, details?: Readonly<Record<string, unknown>>): never => { throw new KitchenDomainError(code, message, details); };
